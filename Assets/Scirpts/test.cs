using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("这是一个初始化代码文件");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
